// Warm Up Questions
// What would the state syntax look like for for the state of “age”
const [stateAge, setStateAge] = useState(age)
// What is the use of map ?
// Map will make a copy of an array and apply our own twist to it by using a callback function that takes in each element of the array then returns what we want to do to each element instead of having to use a for loop to iterate.
// What synthetic event is used to listen to an on click ?? Make sure to get the correct syntax 🐪
// onClick = { <<js function>>}
