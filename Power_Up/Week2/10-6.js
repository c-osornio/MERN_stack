// What is the synthetic event used for submitting a form 🐪
// onSubmit = { <<function>> }
// Why is lifting state, what I mentioned as passing down state, so important ? 
// So that developers are able to pass down values/attributes between components 
// Why do we use e.preventDefault()
//  we must prevent the default refresh of the browser to keep our state from being reset
